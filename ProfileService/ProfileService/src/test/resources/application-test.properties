# --- AWS Configuration for Test Profile ---

# These dummy values are required to resolve the @Value annotations in DynamoDbLocalConfig
# when the test context initializes. They point to the Testcontainers or Docker setup.
aws.region=us-east-1

# CRITICAL FIX: Dummy credentials required for SDK initialization in tests
aws.access-key-id=test-dummy-key
aws.secret-access-key=test-dummy-secret